version: '3.8'
services:
  recorder-bot:
    build: .
    container_name: recorder_v5
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./recordings:/app/recordings
      - ./splits:/app/splits
    env_file:
      - .env
